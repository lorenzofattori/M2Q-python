def createMessage(messageType, channel, note):
    print(f"messageType: {messageType}, channel: {channel}, note: {note}")

    # check if wingmode is activated (move playback to +10)


#     if


# //first I check wingmode - level and jump to cue, then I create the message
#   if (currentSettings.wingMode == true){              //if wingmode is active use playback 11 - 26
#     playback = playback+10;
#   }
#   if(type == 0){    //Type = 0 is Jump message
#     if(currentSettings.jumpMode == true){
#                   message += playback;       //create the jump message
#                   message += ',',
#                   message += value;
#                   message += ',';
#                   message +='0';
#                   message +='J';

#                   sendudp(message);     //send it
#     }
#   }
